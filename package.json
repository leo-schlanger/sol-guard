{
  "name": "sol-guard",
  "version": "1.0.0",
  "description": "SolGuard - AI Security Platform for the Solana Ecosystem",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "deploy": "pwsh -ExecutionPolicy Bypass -File scripts/deploy.ps1",
    "deploy:unix": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "turbo": "^1.10.12",
    "@types/node": "^20.8.0",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
